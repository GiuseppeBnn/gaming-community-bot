aiogram==3.30.0
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
pydantic-settings==2.7.1
redis==5.2.0
aiohttp==3.10.11
# MTProto client for the /backup chat archive (the Bot API can't read history).
# Pure-Python crypto by default; `cryptg` is an optional speed-up (we don't
# download media, so throughput is not critical).
telethon==1.36.0
