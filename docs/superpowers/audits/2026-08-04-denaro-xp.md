# Repository-wide Money and XP Audit

**Plan:** [`docs/superpowers/plans/2026-08-04-audit-denaro-xp.md`](../plans/2026-08-04-audit-denaro-xp.md)
**Design:** [`docs/superpowers/specs/2026-08-04-audit-denaro-xp-design.md`](../specs/2026-08-04-audit-denaro-xp-design.md)
**Branch:** `test_giu` · **Audit base (HEAD at start):** `c6a7097` · **A.1 prerequisite:** complete
(25 commits, final review clean).

---

## Approved invariants

These architectural contracts are normative (STEERING §22) and every matrix row is judged against them:

- Handlers and lifecycle callers own `commit`; services never commit (sole documented exception: shop
  audit trail).
- Money decisions and arithmetic run in SQL: predicate in `WHERE`, relative arithmetic in `SET`
  (`coins = coins + :delta`), `rowcount == 0` means the race was lost, always with
  `.execution_options(synchronize_session=False)` followed by a targeted refresh.
- Reads of decisive values prefer columns over potentially stale ORM entities; never
  `populate_existing=True`, never `session.expire` (async `MissingGreenlet`).
- Locks are used only when atomic SQL is insufficient; canonical order Event → User → Wallet, wallets
  locked by ascending `tg_id`.
- `User.xp` mutates only through `xp_service`; every XP source keeps its capped/uncapped
  classification (recorded in STEERING §12.1).
- Preserved: amounts, prizes, costs, payout formulas, caps, capped/uncapped classifications, ranks,
  thresholds, and every current gameplay outcome. Rebalancing is out of scope.
- `TEST_PG_URL` is only ever the dedicated throwaway container; `DB_URL` and the Compose `gamingbot`
  database are never used by tests. The fixture refuses any database not ending in `_test`.
- A fix requires a focused test observed RED; no production change from inspection alone. Verified-safe
  paths are documented and not rewritten for stylistic uniformity.

## Infrastructure

- Container name: `gaming-community-bot-pg-test` (no volume, `--rm`)
- Image: `postgres:16-alpine` · Database: `gamingbot_test`
- Port: host `127.0.0.1:5433` → container `5432`
- Test DSN: `postgresql+asyncpg://postgres:postgres@localhost:5433/gamingbot_test`
- Safety barrier proven: `TEST_PG_URL=.../gamingbot` fails setup with
  "database is 'gamingbot', which does not end in '_test'" before any schema mutation.

## Baselines

### Default (SQLite) — captured on `c6a7097`

| Check | Result |
|---|---|
| `pytest` | 2283 passed, 30 skipped (PostgreSQL-dependent, no `TEST_PG_URL`) in 46.50s |
| `pytest --cov=src --cov-report=term-missing` | 9199 statements, 57 missing, 99.38%; ratchet ≥99 met |
| `ruff check src/ tests/` | clean |
| `mypy` | Success: no issues found in 37 source files |
| `PYTHONPATH=src python -c 'import main'` | exit 0 |
| Tools | Python 3.12.13 · pytest 8.3.4 · ruff 0.16.0 · mypy 2.3.0 |

### PostgreSQL (dedicated container) — captured on `c6a7097`

| Check | Result |
|---|---|
| `pytest -m pg` with `TEST_PG_URL` | 31 passed |
| Full `pytest` with `TEST_PG_URL` | 2313 passed, 0 skipped in 139.12s |

Both summaries recorded; neither baseline revealed a red test.

---

## Matrix

Columns: `ID` · `Path/function` · `Asset/state` · `Transaction owner` · `SQL decision/arithmetic` ·
`Lock/order` · `Identity-map handling` · `Idempotency/claim` · `Ledger/audit parity` ·
`Rollback behavior` · `SQLite evidence` · `PostgreSQL evidence` · `Status/finding`.

| ID | Path/function | Asset/state | Transaction owner | SQL decision/arithmetic | Lock/order | Identity-map handling | Idempotency/claim | Ledger/audit parity | Rollback behavior | SQLite evidence | PostgreSQL evidence | Status/finding |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CORE-1 | `economy_service.credit` (economy_service.py:95-121) | Wallet.coins (+), ledger `to_tg_id +amount` | handler/lifecycle (economy.py:112/133/217/282, admin:154, admin_dashboard) | existence check then relative `update(Wallet).values(coins=Wallet.coins+amount)` `synchronize_session=False` | none (single-row atomic) | `refresh(wallet,["coins"])` after write | additive; caller-guarded claims | 1 ledger row per credit | exception → nothing before handler commit | test_economy_service (ledger entry, truncation) | TestDebit concurrent deltas sum (credit side) | SAFE |
| CORE-2 | `economy_service.debit` (economy_service.py:124-154) | Wallet.coins (-), ledger `from_tg_id -amount` | shop.py:248/377, betting.py:718, admin:154, admin_dashboard | overdraw guard is the UPDATE predicate: `where(tg_id, Wallet.coins>=-delta)` + `values(coins=Wallet.coins+delta)`; `rowcount==0` → `InsufficientFundsError` | none (atomic SQL) | refresh before report → error shows real balance | n/a (each call is a movement) | ledger row only on rowcount>0 | no partial: single stmt + ledger after success | test_economy_locking (reject zero/negative), economy_service negative ledger | TestDebit.cannot_overdraw_stale; concurrent_debits_no_lost_update | SAFE |
| CORE-3 | `economy_service.transfer` (economy_service.py:161-219) | two Wallets, transfer_in/out row pair, User.transfers_made | economy.py:213-217 (handler) | lock both rows, atomic debit then credit, relative `transfers_made+1` | two wallets `FOR UPDATE` by **ascending tg_id** (sorted) — canonical order | refresh after each write | n/a single call; opposite transfers serialized by locks | pair sums to zero (`+amount`/`-amount`) | SelfTransfer/Insufficient/ValueError/WalletNotFound raised before/at debit → nothing moves | test_economy_service (two ledger entries, transfers_made, self/overdraw refused) | TestTransfer.concurrent_conserve; opposite_no_deadlock; cannot_overdraw_stale | SAFE |
| CORE-4 | `economy_service.claim_daily` + `handlers/economy.py cmd_daily` (economy.py:108-135) | User.last_daily_claim, daily_streak, Wallet.coins (+), ledger daily_reward, capped XP + milestones | handler `/daily` -- **one transaction since fix 6e40524** | single conditional UPDATE (never/not-today-AND-min-gap) + `daily_streak=case(...)`; `rowcount==0` → `DailyAlreadyClaimedError`; reward credit relative | none (atomic conditional UPDATE) | refresh user before reading streak/countdown | claim marker is the idempotency key (WHERE from stored column) | 1 daily_reward row; XP/milestones same txn | whole unit rolls back on any failure; claimed-error branches write nothing | TestDaily (pay/refuse/level-up/group split), locking 2nd claim rejected | TestDailyClaim.double_claim_refused; streak_not_double; TestDailyXpCap.cap_not_bypassed | SAFE (post 6e40524) |
| CORE-4a | `/daily` claim-vs-XP boundary (`handlers/economy.py` pre-fix) | claim reward vs capped XP/milestone side effects | handler | n/a (transaction-boundary defect, not SQL) | n/a | n/a | claim marker blocks retry | reward committed without XP on failure | pre-fix: 2 commits (`:112`,`:133`); failure between → coins+marker kept, XP lost | reproduction `TestDaily.test_a_failed_xp_side_effect_discards_the_claim` RED 2/2 pre-fix | n/a (deterministic, SQLite) | DEFECT → FIXED (`6e40524`); reproduction kept green |
| CORE-5 | `admin_service.set_balance` (admin_service.py:35-59) | Wallet.coins absolute target, ledger admin_credit/debit delta | admin.py:173-182, admin_dashboard.py:537/603 | `lock_balance` (SELECT coins FOR UPDATE) → delta via credit/debit (relative stmts) | single wallet row lock (absolute target needs current value) | lock reads **column**, not entity → committed value under lock | n/a | 1 ledger row for the delta | InsufficientFunds/ValueError caught by handler, no commit | test_admin_service, test_admin_commands | TestAdminSetBalance.lands_on_requested_target; cannot_overdraw_stale | SAFE |
| CORE-6 | `admin_service.mass_credit` (admin_service.py:62-87) | every Wallet.coins + per-wallet ledger row | admin.py:277-282, admin_dashboard.py:224-228 | one relative `update(Wallet).values(coins=Wallet.coins+amount)` + `add_all` one row per tg_id | none (relative arithmetic, lossless) | bulk stmt; callers don't re-read stale wallets | n/a | exactly 1 ledger row per wallet (`len(tg_ids)`) | handler; empty set → 0 without writes | test_admin_service, test_admin_commands | (relative arithmetic is lossless; no separate PG race) | SAFE |
| CORE-7 | wallet creation `db_middleware._upsert_user` (db_middleware.py:40-74), `admin_service.set_user_banned:285` | new Wallet(tg_id, coins=0) | middleware (grant before handler) / admin | SELECT + add + commit | n/a (creation) | n/a | upsert (first contact; defensive re-create) | none | middleware commits before handler; handler txn separate | test_economy_handlers (missing wallet reported) | TestMiddlewareDoesNotPoisonTheSession.upsert_leaves_no_entities | SAFE |
| CORE-8 | handler money commit/rollback ownership (economy.py, admin.py, admin_dashboard.py) | all CORE txns | handlers own commit; services never commit on money paths | covered per CORE-1..6 | n/a | n/a | n/a | n/a | exceptions caught → no commit; middleware close discards uncommitted unit | handler tests (no poison) | middleware identity-map PG test | SAFE |
| SHOP-1 | cosmetic purchase `cb_buy` (shop.py:225-260) | Wallet.coins (-), ShopPurchase(success=True), active tags | handler `shop.py` (commit 248) | pre-check `has_cosmetic` → `debit` (atomic overdraw guard, takes wallet row lock) → re-check ownership → `record_purchase` + `apply_cosmetic` | wallet row lock via leading debit UPDATE (serializes same-user concurrent buys) | `debit` refreshes Wallet.coins; re-check reads committed rows after lock | double-purchase: pre-check + post-debit re-check; duplicate → `rollback` (never charge twice) | 1 ledger row + 1 ShopPurchase row per pay | `InsufficientFundsError`/`WalletNotFoundError` caught, no commit → no debit/no row | test_shop_handlers, test_shop_service (idempotency) | TestShopDoublePurchase.test_a_double_tap_pays_for_the_cosmetic_once | SAFE |
| SHOP-2 | consumable purchase `cb_cbuy` (shop.py:360-400) | Wallet.coins (-), ShopPurchase row (repeatable) | handler `shop.py` (commit 377) | `debit` (overdraw guard) → `record_consumption` → `flush` → `check_and_award_milestones` → commit | wallet lock via leading debit; consumables repeatable (each charge dedicated) | debit refresh; milestone count SELECT sees flushed row (autoflush off) | n/a — repeatable by design; per-purchase charge | 1 ledger + 1 ShopPurchase per purchase | insufficient → raise before any row; no commit | test_consumable_service, test_shop_handlers (pantry counts) | (consumables race covered by debit lock semantics) | SAFE |
| SHOP-3 | ownership / tag switcher (`shop_service.has_cosmetic/owned_cosmetic_keys/toggle_tag/ensure_active_seeded/apply_cosmetic`) | User.active_tags_json, cosmetic_tag, ShopPurchase | handler (shop commit 424) | SELECT + attribute mutations, no money | none | n/a | `has_cosmetic` = ownership idempotency key; legacy single-tag migration once | n/a (no ledger) | ensure/toggle straight-line; handler commit owns | test_shop_service (switcher/toggles), test_shop_home_balance | n/a | SAFE |
| SHOP-4 | milestone reads & balance trophies (`badge_service.check_and_award_milestones`, `progress_service`, `consumable_service` counts) | trophy awards + aggregate reads (no money) | handler `/daily`, `cb_cbuy` | counter conditions read User/Wallet: `balance` reads `user.wallet.coins` — post-debit truth via the Wallet instance refreshed by `debit` | n/a | selectinload returns identity-map instance whose Wallet was refreshed | awards once (earned_ids guard); progress events idempotent on (user, metric, ref) | Badge.xp_reward is display-only per STEERING §12 → NOT_APPLICABLE (trophies never mutate XP) | trophies award only; no rollback concerns | test_badge_service, test_progress_service, test_consumable_service gift paths | n/a | SAFE |
| BET-1 | `bet_service.place_bet` (bet_service.py:149-225) | event status, Wallet.coins (-), BettingOption.total_wagered, UserBet row, uncapped XP | betting.py:717-718 (handler commit) | event `FOR UPDATE` → status must be `open` + window check → `debit` (atomic overdraw guard) → atomic `total_wagered+amount` → add UserBet → flush (IntegrityError → `AlreadyBetError`) → uncapped participant XP | **Event first**, then Wallet (debit); uncapped XP takes no User lock (documented non-inversion) | debit refreshes Wallet; collection not trusted for decisive reads | 1 bet per (user,event): unique key + IntegrityError rollback; window honoured to the second via `closes_at` | 1 `bet_placed` ledger row per bet | `BettingClosedError`/`EventNotFoundError`/`InsufficientFundsError` → nothing moves | test_bet_service, test_bet_locking, test_betting_handlers | TestBettingLifecycle.bet_placed_before_resolution_is_settled | SAFE |
| BET-2 | `BettingOption.total_wagered` (bet_service.py:189-193) | atomic wager total per option | same as BET-1 (handler) | relative `update(...).values(total_wagered=total_wagered+amount)` — no read-check-write | none (atomic SQL) | n/a (column arithmetic) | concurrent bets each add; no lost update | totals feed payout basis only (not a wallet) | n/a | test_bet_service (sums) | concurrent bets race (services lossless) | SAFE |
| BET-3 | `bet_service.resolve_event` (bet_service.py:266-404) | status→resolved, per-bet win/lose, bets_won counter, XP, payouts + bet_won ledger | admin_betting.py:334-338 (handler); never scheduled automatically | conditional `UPDATE ... WHERE status NOT IN (resolved,cancelled)` is the payout guard (rowcount 0 → `EventAlreadySettledError`); pending bets **re-queried** (fresh, not loaded collection); proportional payout with floor + rounding leftover to biggest winner; per winner: `User FOR UPDATE` → `bets_won+=1` → uncapped `grant_xp(bet_won)` → `credit(payout)` | **Event → User → Wallet** (event lock taken; User locked before Wallet credit) | `session.refresh(event, [...])` after the guard; fresh pending-bet query | double resolution pays once (state claim in SQL); one win/XP credit per bet | entire pot distributed to winners (`total_distributed == total_pot` when pool>0); `bet_won` ledger per payout | whole resolve is one handler txn: status/XP/credits all roll back together on failure | test_bet_service, test_bet_locking, test_admin_betting (payout halves/leftover) | TestBettingLifecycle.double_resolution_pays_once_from_a_clean_session | SAFE |
| BET-4 | `bet_service.cancel_event` (bet_service.py:407-445) | status→cancelled, refunds + bet_refund ledger | admin_betting.py:454-456 (handler) | event `FOR UPDATE` + refuse settled; refund each pending bet via `credit(bet.amount)` | Event lock first (serializes place_bet) | collection loaded under event lock within same txn; no new bet can insert after (place_bet needs event lock) | refunds only `pending` bets once; settled events refused | 1 `bet_refund` ledger per refunded bet | handler commit; failure rolls back status + refunds | test_bet_service, test_admin_betting (refund) | (serialized by event lock; deterministic SQLite path) | SAFE |
| BET-5 | scheduler vs admin parity (bet_type.py `_auto_lock` :99-111) | auto-lock = same guard as admin lock | scheduler loop (schedule.py:436 commit) | scheduler only **auto-locks** (never resolves/cancels); calls `bet_service.lock_event` (conditional `WHERE status=open`) exactly like the admin lock | event row lock/guard shared | TaskSkip on already-closed | idempotent lock (open→locked once) | n/a | per-task rollback in scheduler; failure isolated per task | test_bet_type_scheduled | TestBettingLifecycle.second_lock_does_not_move_close_time | SAFE |
| BET-6 | handler ownership (betting.py:717-718, admin_betting.py:338/456, schedule.py:436) | commits for place/resolve/cancel/auto-lock | handlers + scheduler own commit; services never commit | covered per BET-1..5 | n/a | n/a | n/a | n/a | notifications after commit (best-effort send) never undo/repeat | handler tests | n/a | SAFE |

(rows are added in Tasks 2–8; no row may remain `NEEDS_TEST` when Task 8 closes)

---

## Source index

### 2.1 Direct money expressions (Task 2 Step 1)

Every direct `Wallet.coins` / `User.xp` / ledger / award expression found in `src/` is classified
and mapped to the owning audit task. Presentation-only reads and catalog constants are
`NOT_APPLICABLE` and listed for completeness.

#### `Wallet.coins` and `coins` arithmetic

| Location | Kind | Owner |
|---|---|---|
| `services/economy_service.py:51` | `select(Wallet.coins)` decisive read | CORE-1/2 |
| `services/economy_service.py:74` | docstring — SQL semantics `coins = coins + :delta` | CORE-1/2 |
| `services/economy_service.py:86,88` | debit SQL predicate `Wallet.coins >= -delta` + relative `values(coins=Wallet.coins + delta)` | CORE-2 |
| `services/admin_service.py:77` | `set_balance` absolute `values(coins=Wallet.coins + amount)` | CORE-5 |
| `services/admin_service.py:122,145,160,162,179,193,194` | admin stats/reads (incl. `sum(Wallet.coins)`) | NOT_APPLICABLE (reads) |
| `services/admin_service.py:285` | wallet creation `coins=0` | CORE-7 |
| `services/quiz_service.py:71,680,687,705` | prize-coins computation for podium schedule | QUIZ-2/3 |
| `services/guess_service.py:691,694` | prize-coins computation for standings | GUESS-2/3 |
| `services/badge_service.py:365` | XP-ordered read for trophy leaderboards | NOT_APPLICABLE (read) |
| `handlers/admin_dashboard.py:313,314` | admin dashboard balance read | NOT_APPLICABLE (read) |
| `middlewares/db_middleware.py:54,73` | wallet upsert creation `coins=0` | CORE-7 |

#### `User.xp`, `xp_today`, `xp` arithmetic

| Location | Kind | Owner |
|---|---|---|
| `services/xp_service.py:189,193` | daily-cap reset read (refresh + `xp_today_date` guard) | XP-5 |
| `services/xp_service.py:213–215` | capped grant: relative `xp=User.xp + granted`, `xp_today=spent_today + granted` | XP-1 |
| `services/xp_service.py:218` | uncapped grant: relative `xp=User.xp + granted` | XP-2 |
| `services/xp_service.py:243` | `set_xp` absolute write `user.xp = max(0, value)` | XP-4 |
| `services/xp_service.py:262` | `airdrop_xp` relative `xp=User.xp + amount` | XP-3 |
| `services/xp_service.py:271,288` | XP leaderboard reads | NOT_APPLICABLE (read) |
| `database/models.py:75,81,82`, `database/connection.py:46,47` | schema/migration declarations | NOT_APPLICABLE (DDL) |

All `User.xp` mutations live inside `src/services/xp_service.py` (single-place invariant, §7.1).

#### `LedgerEntry`, `TransactionType`, XP entry points

| Location | Kind | Owner |
|---|---|---|
| `services/economy_service.py:114,147` | credit/debit ledger rows | CORE-1/2 |
| `services/economy_service.py:198,203` | transfer out/in paired ledger rows | CORE-3 |
| `services/economy_service.py:319` | daily-reward ledger row | CORE-4 |
| `services/admin_service.py:53,57,79,82` | `set_balance` ledges (`admin_credit`/`admin_debit`) | CORE-5 |
| `services/admin_service.py` (mass credit path) | per-wallet ledger row | CORE-6 |
| `services/bet_service.py:183,386,435` | bet placed / bet won / bet refund ledger | BET-1/3/4 |
| `services/quiz_service.py:677` | quiz-reward ledger | QUIZ-2 |
| `services/guess_service.py:688` | guess-reward ledger | GUESS-2 |
| `services/guess_service.py:630,653,660` | guess XP grant (uncapped) | GUESS-3 |
| `services/quiz_service.py:618,642,652` | quiz XP grant (uncapped) | QUIZ-3 |
| `services/bet_service.py:220,376` | participant/winner XP grant (uncapped) | BET-1/3 |
| `services/xp_service.py:155,235,248` | `grant_xp` / `set_xp` / `airdrop_xp` definitions | XP-1..4 |
| `handlers/economy.py:36–47,79,80` | display mapping / history filter | NOT_APPLICABLE (read) |
| `handlers/shop.py:237,369` | shop debit ledger description | SHOP-1/2 |
| `handlers/economy.py:126,279` | daily XP grant / manual admin credit call site | CORE-4 / task 3 |
| `handlers/admin.py:141,201,227` | admin debit / grant / set call sites | CORE/XP-7 |
| `handlers/admin_dashboard.py:252,499,503,520–537` | airdrop / admin grant / set / credit / debit call sites | XP-3/7 |

#### Award / close / transition entry points

| Location | Kind | Owner |
|---|---|---|
| `services/quiz_service.py:278,655` | `claim_close` / `award_prizes` definitions | QUIZ-2 |
| `services/guess_service.py:174,664` | `claim_close` / `award_prizes` definitions | GUESS-2 |
| `services/bet_service.py:266,407` | `resolve_event` / `cancel_event` definitions | BET-3/4 |
| `services/progress_service.py:54,99` | `record_podium` / `record_event` definitions | QUIZ/GUESS 3 |
| `handlers/admin_betting.py:334,454` | admin resolve / cancel call sites | BET-3/4 |
| `handlers/quiz/lifecycle.py:153,166,177,181,187` | close: claim → prizes → progress | QUIZ-2/3 |
| `handlers/guess/lifecycle.py:129,153,159` | close: claim → prizes → podium | GUESS-2/3 |

### 2.2 Service-call → transaction-owner map (Task 2 Step 2)

| Service call | Committing owner |
|---|---|
| `economy_service.credit/debit/transfer/claim_daily` → `handlers/economy.py` (`/dep` `/pre` `/transfer` `/daily`) | handler (1022, 112, 217, 282 commits) |
| `economy_service.credit/debit` → `handlers/admin.py` `cmd_set_balance`-adjacent manual credit | handler (154) |
| `economy_service.credit/debit` → `handlers/admin_dashboard.py` dashboard money actions | handler (525, 552, 581, 603) |
| `admin_service.set_balance/mass_credit` → `handlers/admin.py` / `handlers/admin_dashboard.py` | handler (182, 282; 228) |
| `bet_service.place_bet` → `handlers/betting.py` | handler (718) |
| `bet_service.resolve_event/cancel_event` → `handlers/admin_betting.py` | handler (338, 456) + `handlers/schedule.py` scheduler (436) |
| `quiz_service.claim_close/award_prizes`, `progress_service.record_*` → `handlers/quiz/lifecycle.py` | handler (139, 197) + scheduler (436) |
| `guess_service.claim_close/award_prizes`, `progress_service.record_*` → `handlers/guess/lifecycle.py` | handler (171) + scheduler (436) |
| `xp_service.grant_xp/set_xp/airdrop_xp` → `handlers/admin.py`, `handlers/admin_dashboard.py`, `handlers/economy.py` | handler (133, 207, 231; 256, 506) |
| `shop_service.record_purchase/apply_cosmetic`, `consumable_service.record_consumption`, `badge_service.check_and_award_milestones` → `handlers/shop.py` | handler (248, 377, 424, 469) |
| `bet/quiz/guess_service` internal `economy_/xp_` calls inside `resolve/close/award` | outer handler or scheduler owns commit |

### 2.3 Service commits flagged for review (Task 2 Step 2)

| Location | Reason | Verdict |
|---|---|---|
| `services/badge_service.py:151` (`sync_trophies`) | catalog-sync at startup; documented exception | ACCEPTABLE (non-money, startup) |
| `services/group_registry.py:95,124` | startup migration bookkeeping + exceptional Telegram chat-migration path | ACCEPTABLE (non-money, dedicated txn) |
| `services/shop_service.py` | **no commit** — docstring "No-commit convention: callers own the transaction"; `record_purchase`/`apply_cosmetic` don't commit | ACCEPTABLE (handler owns) |
| `services/{bet,quiz,guess}_service.py` `session.rollback()` (bet:210, quiz:506, guess:536) | rollback after `IntegrityError` — duplicate-claim signal, session left clean; outer owner still commits | ACCEPTABLE (rollback ≠ commit) |
| `services/backup/chat_archive.py` writer | separate JSON archive writer, own lifecycle | ACCEPTABLE (non-DB-money) |

### 2.4 Reconciliation (Task 2 Step 3)

All four Step-1 searches re-run after reconciliation produced zero unclassified production matches
under `src/`. Counts by domain: core economy/admin currency 8 rows (CORE-1..8, Task 3), shop /
consumables / trophies 4 rows (SHOP-1..4, Task 4), betting 6 rows (BET-1..6, Task 5), quiz/guess
rewards 6 rows (QUIZ-1..3 + GUESS-1..3, Task 6), XP 8 rows (XP-1..8, Task 7). Reads and DDL/catalog
declarations are `NOT_APPLICABLE` and listed above.

---

## Confirmed defects

### D1 — `/daily` claim and its XP side effects were two transactions (CORE-4a, fixed)

- **Evidence:** `TestDaily.test_a_failed_xp_side_effect_discards_the_claim` was RED 2/2 pre-fix: a
  failure between the claim commit and the XP/milestone commit paid out the coins, recorded
  `last_daily_claim` and the ledger, then lost the XP permanently (the marker blocks a retry).
- **Root cause:** `handlers/economy.py` committed the claim (`:112`) before the capped-participation
  XP grant and milestone check (`:126-130` → `:133`), running one logical unit as two transactions.
- **Fix:** `6e40524` — `cmd_daily` now commits once at the end of the unit; the claimed-error
  branches write nothing, so `SAFE`. Reproduction kept permanently and green.
- **Plan task:** `docs/superpowers/plans/2026-08-04-audit-denaro-xp.md` Task 3A.

Baseline had no confirmed defects; each later confirmed defect gets a `DEFECT` matrix row, a RED
reproduction that stays permanently, an appended fix task, and a documented fix commit.
